let initialState = {
  friends: [
    { id: 1, name: 'Tony' },
    { id: 2, name: 'Kate' },
    { id: 3, name: 'Anna' },
  ],
};

const sidebarReducer = (state = initialState, action = null) => {
  return state;
};

export default sidebarReducer;
